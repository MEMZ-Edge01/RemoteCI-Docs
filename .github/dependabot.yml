version: 2
updates:
  # GitHub Actions 与 npm（pnpm lockfile）依赖每周检查一次，自动开 PR。
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
